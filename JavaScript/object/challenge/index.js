
// This is a person object

const person = {
    name: "Niavo",
    age : 22,
    country: "Madagascar",
    
}

function logData () {

  console.log(`${person.name} is ${person.age} years old and lives in ${person.country}`);

}


logData();